# Environments / caches
myenv/
__pycache__/
*.pyc
.DS_Store

# Raw dataset and intermediate artifacts (regenerated by scripts/)
celebrity/
celebrity_faces/
face_models/
/face_recognizer.pkl        # build intermediate; the deployed copy lives in assets/

# Large model weights: downloaded at runtime by recognizer.py
assets/*.onnx

# Streamlit secrets
.streamlit/secrets.toml
